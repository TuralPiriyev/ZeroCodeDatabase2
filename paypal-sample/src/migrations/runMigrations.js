console.log('This project uses MongoDB (mongoose). The SQL migration runner is not required for Mongo.');
console.log('If you need SQL migrations, re-enable the runner and install knex/sqlite3.');
process.exit(0);
